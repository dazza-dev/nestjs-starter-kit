/** Role with full access: its bypass is applied in PermissionsGuard. */
export const ADMIN_ROLE = 'admin';

/** System roles, which cannot be deleted. */
export const SYSTEM_ROLES: string[] = ['admin', 'user'];
